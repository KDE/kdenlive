PROJECT(KDENLIVE)

# Kdenlive version string
SET (VERSION_MAJOR "0")
SET (VERSION_MINOR "6")
SET (VERSION_PATCH "0")
# comment out the next line for a public release
SET (VERSION_SUFFIX "svn")
SET (VERSION ${VERSION_MAJOR}.${VERSION_MINOR}.${VERSION_PATCH})
IF(VERSION_SUFFIX)
   SET (VERSION "${VERSION}-${VERSION_SUFFIX}")
ENDIF(VERSION_SUFFIX)
ADD_DEFINITIONS( ${CMAKE_CXX_FLAGS} -D'VERSION="${VERSION}"')

SET(CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/cmakemodules ${CMAKE_SOURCE_DIR}/cmake/modules)

INCLUDE(FindGettext)
INCLUDE(KdenlivePackageMessages)

# CHECK DEPENDENCIES
FIND_PACKAGE(LIBMLT REQUIRED)
FIND_PACKAGE(LIBMLTPLUS REQUIRED)
FIND_PACKAGE(LIBFFMPEG REQUIRED)
# END DEPENDENCIES


#ADD_DEFINITIONS ( CXXFLAGS="$CXXFLAGS -DMLT_PREFIX=\\\"\"MLT_PATH\"\\\"" )
#SET(CMAKE_C_FLAGS `mlt-config --cflags`)


SUBDIRS(kdenlive renderer graphics profiles pgm icons doc effects po)

