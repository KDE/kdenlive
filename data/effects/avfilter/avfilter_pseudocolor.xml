<?xml version="1.0"?>
<!DOCTYPE kpartgui>
<effect xmlns="https://www.kdenlive.org" tag="avfilter.pseudocolor" id="avfilter.pseudocolor">
    <name>Pseudocolor</name>
    <description>Alter frame colors in video with pseudocolors.</description>
    <author>libavfilter</author>
    <parameter type="list" name="av.index" default="0" paramlist="0;1;2;3">
        <name>Base Component</name>
        <paramlistdisplay>Red,Green,Blue,Alpha</paramlistdisplay>
    </parameter>
    <parameter type="list" name="av.preset" default="0" paramlist="none;magma;inferno;plasma;viridis;turbo;cividis;range1;range2;shadows;highlights;solar;nominal;preferred;total;spectral;cool;heat;fiery;blues;green;helix">
        <name>Preset</name>
        <paramlistdisplay>None,Magma,Inferno,Plasma,Viridis,Turbo,Cividis,Range 1,Range 2,Shadows,Highlights,Solar,Nominal,Preferred,Total,Spectral,Cool,Heat,Fiery,Blues,Green,Helix</paramlistdisplay>
    </parameter>
    <parameter type="animated" name="av.opacity" min="0" max="100" default="1" factor="100" suffix="%">
        <name>Opacity</name>
    </parameter>
<!--
The effect/filter works with the parameters above but can theoretically manage more complex expressions.

Hidden parameters:
c0 - set pixel first component expression
c1 - set pixel second component expression
c2 - set pixel third component expression
c3 - set pixel fourth component expression, corresponds to the alpha component

Each of the expression options specifies the expression to use for computing the lookup table for the corresponding pixel component values.

The expressions can contain the following constants and functions: 
w, h - The input width and height.
val - The input value for the pixel component.
ymin, umin, vmin, amin - The minimum allowed component value.
ymax, umax, vmax, amax - The maximum allowed component value. 
-->
</effect>
